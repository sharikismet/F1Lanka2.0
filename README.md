
  # F1Lanka store new patch

  This is a code bundle for F1Lanka store new patch. project built by sharik ismet 
  version 2.0 released on 3rd April 2026.
  
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  